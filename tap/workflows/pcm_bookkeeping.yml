name: PCM Bookkeeping Compatibility Route
version: "3.0"
description: "Legacy DB-selected route delegated to the canonical incremental PCM workflow; contains no export or close step."
trigger_topic: events.accounting.1.pcm_bookkeeping
steps:
  - id: canonical_pcm_flow
    sub_workflow: "PCM Statement Intake and Posting"
    negotiate: false
    timeout: "900s"
    description: "Delegate unchanged input to the canonical Stage-2 review and Shadow ERP posting workflow."
